import React, {Component} from "react";
import "./App.css";

const Header = () => {
    return (
        <header>
            <img src="fn-logo.png" alt="Field Nation"/>
        </header>
    );
};

export default Header;
